# Security

Security requirements are defined by `docs/ARCHITECTURE.md`, section 17.

The implementation must treat analyzed source code as untrusted data and must keep GigaChat credentials only on the backend side.

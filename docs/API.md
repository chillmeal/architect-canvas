# API

The MVP HTTP API is defined by `docs/ARCHITECTURE.md`, section 15.

Pydantic models in `apps/api` are the source of OpenAPI contracts. The snapshot in `contracts/openapi.snapshot.json` is a bootstrap placeholder until generated contracts are introduced.

openapi: 3.0.0
info:
  title: Billing API
  version: 1.0.0
paths:
  /invoices/{orderId}:
    get:
      operationId: getInvoiceForOrder
      responses:
        "200":
          description: Invoice status

services:
  api:
    build:
      context: ./apps/api
    env_file:
      - .env
    environment:
      DATABASE_URL: sqlite:///./var/data/app.db
    ports:
      - "127.0.0.1:8000:8000"
    volumes:
      - ./var:/app/var
